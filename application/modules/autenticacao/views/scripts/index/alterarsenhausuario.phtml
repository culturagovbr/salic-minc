<script>

    $(document).ready( function() {
	$("#alterarSenha").validate({
		// Define as regras
		rules:{
                        senhaAtual:{
				// campoNome será obrigat�rio (required) e ter� tamanho m�nimo (minLength)
				required: true, minlength: 2
			},
			senhaNova:{
				// campoNome será obrigat�rio (required) e ter� tamanho m�nimo (minLength)
				required: true, minlength: 2
			},
			repeteSenha:{
				// campoEmail será obrigat�rio (required) e precisar� ser um e-mail v�lido (email)
				required: true, minlength: 2
			}

		},
		// Define as mensagens de erro para cada regra
		messages:{
                        senhaAtual:{
				required: "Digite a senha atual",
				minLength: "O seu nome deve conter, no m�nimo, 11 caracteres"
			},
			senhaNova:{
				required: "Digite senha nova",
				minLength: "O seu nome deve conter, no m�nimo, 2 caracteres"
			},
			repeteSenha:{
				required: "Repetir senha nova",
                                minLength: "O seu nome deve conter, no m�nimo, 2 caracteres"

			}
		}
	});
});

function validaSenha(){

    var data = document.getElementById("senhaAtual").value;
    //var iChars = "<";
    data = data.replace("<", "##menor##");
    data = data.replace(">", "##maior##");
    data = data.replace("'", "##aspa##");

    document.getElementById("senhaAtual").value = data;
    //document.alterarSenha.submit();
    return true;
}


</script>
<div id="confirm"></div>
<div id="breadcrumb">
<?php
$auth = Zend_Auth::getInstance();

if ( isset ($auth->getIdentity()->Cpf ) ) { ?>
    <ul>
        <li class="first"><a href="<?php echo $this->url(array('module'=>'default', 'controller' => 'principalproponente', 'action' => 'index')); ?>">In&iacute;cio</a></li>
        <li class="last">Cadastrar Usu�rio</li>
    </ul>
<?php } ?>
</div>

<div id="titulo">
    <div>Alterar Senha<span></span> <span class="voltar"><a href="#" onclick="voltar();">Voltar</a></span></div>
</div>
	<div id="conteudo">
            <form method="POST" onsubmit="return validaSenha()" action="<?php echo $this->url(array('module'=>'autenticacao', 'controller' => 'index', 'action' => 'alterarsenhausuario')); ?>" name="alterarSenha" id="alterarSenha">
                <table border="1" class="tabela">
                    <thead>
                        <tr>
                            <th colspan="2">Informa��es</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td width="150" class="destacar" align="right">Senha Atual <span class="red">*<span></td>
                            <td><input maxlength="50" class="input_simples" type="password" name="senhaAtual" id="senhaAtual" /></td>
                        </tr>
                        <tr>
                            <td class="destacar" align="right">Senha nova <span class="red">*<span></td>
                            <td><input maxlength="50" class="input_simples" type="password" name="senhaNova" id="senhaNova" /></td>
                        </tr>
                        <tr>
                            <td class="destacar" align="right">Repetir senha nova <span class="red">*<span></td>
                            <td><input maxlength="50" class="input_simples" type="password" name="repeteSenha" id="repeteSenha" /></td>
                        </tr>
                        <tr>
                            <td align="center" colspan="2">
                                <button id='btIncluir' type='submit'  value='' class='btn enviar' >salvar</button>
                                <button id='btLimpar' type='reset' value='' class='btn enviar white black-text' >limpar</button>
                                <button id='btCancelar' type='button' value='' class='btn enviar white black-text' onClick="history.go(-1)" onkeypress=" ">cancelar</button>
                            </td>
                        </tr>
                    </tbody>
                </table>
                <input id='idUsuario' name="idUsuario" type='hidden' value='<?php echo $this->idUsuario; ?>' />
                <?php if(isset($_GET['idUsuario'])){ ?>
                <input id='idUsuario' name="idUsuarioGet" type='hidden' value='<?php echo $_GET['idUsuario']; ?>' />
                <?php } ?>
            </form>
        </div>

<!-- ========== FIM CONTEÚDO ========== -->



<!-- ========== INÍCIO RODAPÉ DO CONTEÚDO ========== -->
	<div id="rodapeConteudo"><span></span></div>

<!-- ========== FIM RODAPÉ DO CONTEÚDO ========== -->
			<br clear="all" />
